2 3 * str wl
1.5 2.5 * str wl
5 2 / str wl
5.0 2.0 / str wl
# Mixed-type arithmetic is rejected at runtime (and statically by --check-types).
# Use toFloat / toInt to convert explicitly:
#   5 toFloat 2.0 /
#   5.0 toInt 2 /

-3 abs str wl

2 3 - str wl

1.5 0.5 - str wl
